import { FinalpricePipe } from './finalprice.pipe';

describe('FinalpricePipe', () => {
  it('create an instance', () => {
    const pipe = new FinalpricePipe();
    expect(pipe).toBeTruthy();
  });
});
